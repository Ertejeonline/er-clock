# ER Clock

A simple clock app for Evenrealities G2 glasses.

## Development

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`
3. Build: `npm run build`
4. Pack: `npm run pack`